syntax = "proto3";
option optimize_for = LITE_RUNTIME;

package tutorial;

message BasicMessage
{
  // send and recv proxy
  string send = 1;
  string recv = 2;
  // message type
  string type = 3;
  // true message
  bytes message = 4;
}
